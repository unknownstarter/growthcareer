# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
build/

# Production
dist/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env*.local
.env.local
.env.development.local
.env.production.local

# Vercel
.vercel

# Claude Code (per-developer permissions)
.claude/settings.local.json
.claude/scheduled_tasks.lock
docs/screenshots/

# Supabase CLI runtime artifacts
supabase/.temp/
supabase/.branches/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo

# 강사 원본 프로필 자료 (개인정보 — 로컬 보관, 가공된 사진만 public/ 에 커밋)
src/programs/fan-to-pro/*.pdf
src/programs/fan-to-pro/*.xlsx
src/programs/fan-to-pro/준컴퍼니_*.png
src/programs/fan-to-pro/준컴퍼니_*.jpeg

# 내부 손익 분석 / 사업 문서 (노션 임포트용 — 레포에 커밋 금지)
Fan to Pro 1기 손익분석.md
*손익분석*.md
*시장조사*.md
*사업기회*.md

# 학생/지원자 PII (이름·연락처·생년·국적 등 — 로컬 운영용, 커밋 금지)
docs/private/

# 내부 전용 문서 (외부 공유 금지 — git 추적 제외)
docs/internal/

# GA4 서비스계정 키 (절대 커밋 금지)
ga4-sa.json
*-sa.json
*.ga4.json

# 디자인 프리뷰 캡처 (재생성 가능, 20MB+ - 로컬 보관)
docs/design/captures/

# 1기 현장 원본 사진/영상 (수강생 얼굴 = PII, 78MB - 가공본만 public/ 에 커밋)
src/programs/fan-to-pro/assets/

# 설문 원본 응답 + 외부 제공 교육자료 (응답자 PII / 저작권)
docs/*.csv
docs/*.pdf

# 카드뉴스 렌더 소스 (인스타 게시용 - 커밋 금지)
docs/marketing/

# 설치형 스킬 본문 (skills-lock.json 만 추적)
.agents/skills/
.claude/skills/
